name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  # Build Cross-Platform Binaries
  # Builds both fountain-encode and fountain-decode for all platforms
  build-binaries:
    name: Build Binaries ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            suffix: linux-x86_64
          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            suffix: linux-aarch64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            suffix: windows-x86_64
          - os: macos-latest
            target: x86_64-apple-darwin
            suffix: macos-x86_64
          - os: macos-latest
            target: aarch64-apple-darwin
            suffix: macos-aarch64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools (Linux)
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools
          if [ "${{ matrix.target }}" = "aarch64-unknown-linux-musl" ]; then
            sudo apt-get install -y gcc-aarch64-linux-gnu
          fi

      - name: Configure cargo for ARM64 cross-compile
        if: matrix.target == 'aarch64-unknown-linux-musl'
        run: |
          mkdir -p .cargo
          cat > .cargo/config.toml << EOF
          [target.aarch64-unknown-linux-musl]
          linker = "aarch64-linux-gnu-gcc"
          EOF

      - name: Build Binaries
        run: cargo build --release --target ${{ matrix.target }}

      - name: Strip binaries (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          if [ "${{ matrix.target }}" = "aarch64-unknown-linux-musl" ]; then
            aarch64-linux-gnu-strip target/${{ matrix.target }}/release/fountain-encode
            aarch64-linux-gnu-strip target/${{ matrix.target }}/release/fountain-decode
          else
            strip target/${{ matrix.target }}/release/fountain-encode
            strip target/${{ matrix.target }}/release/fountain-decode
          fi

      - name: Prepare Artifact
        shell: bash
        run: |
          mkdir dist
          EXE_EXT="${{ matrix.os == 'windows-latest' && '.exe' || '' }}"
          cp target/${{ matrix.target }}/release/fountain-encode$EXE_EXT dist/fountain-encode-${{ matrix.suffix }}$EXE_EXT
          cp target/${{ matrix.target }}/release/fountain-decode$EXE_EXT dist/fountain-decode-${{ matrix.suffix }}$EXE_EXT

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.target }}
          path: dist/*

  # Build WASM
  build-wasm:
    name: Build WASM
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-bindgen-cli
        run: cargo install wasm-bindgen-cli --version 0.2.122

      - name: Build WASM
        run: |
          cargo build --release --target wasm32-unknown-unknown --no-default-features --features wasm
          wasm-bindgen target/wasm32-unknown-unknown/release/fountain_core.wasm --out-dir www/pkg --target web

      - name: Package WASM
        run: |
          cd www
          zip -r ../fountain-wasm.zip pkg/ index.html main.js

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: wasm-pkg
          path: fountain-wasm.zip

  release:
    name: Create Release
    needs: [build-binaries, build-wasm]
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          mkdir release_assets
          find artifacts -type f -not -path "*/.*" -exec cp {} release_assets/ \;
          ls -R release_assets

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          generate_release_notes: true
          files: release_assets/*
